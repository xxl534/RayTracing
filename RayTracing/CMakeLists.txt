AUX_SOURCE_DIRECTORY(. SRC)
AUX_SOURCE_DIRECTORY(hitable SRC)
AUX_SOURCE_DIRECTORY(material SRC)
AUX_SOURCE_DIRECTORY(pdf SRC)

FILE(GLOB_RECURSE INC "*.h")
FILE(GLOB_RECURSE INC_TEMP "*.inl")
LIST(APPEND INC ${INC_TEMP})

ADD_STATIC( RayTracing "${INC}" "${SRC}" )